name: 'copy tag'
description: 'add tag for exist image:tag'
inputs:
  registry: 
    description: 'registry'
    required: true
  registry-user: 
    description: 'registry'
    required: true
  registry-password: 
    description: 'registry'
    required: true
  tag:
    description: 'tag'
    required: true
  repo-name:
    description: 'override repo name'
    required: false
    default: ''
  image-platform:
    description: 'image platform'
    required: false
    default: 'linux/amd64'
  images:
    description: 'list images in Yaml/JSON format'
    required: true

outputs:
  metadata:
    description: 'metadata'
  labels:
    description: 'labels'


runs:
  using: 'node24'
  main: 'dist/main.js'